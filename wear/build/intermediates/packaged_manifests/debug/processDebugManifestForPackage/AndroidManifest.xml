<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sleepwisepoc"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="30"
        android:targetSdkVersion="34" />

    <!--
         This is a Wear OS companion APK to the phone app — declare standalone=false
         so Wear OS knows it's paired with a phone app of the same applicationId.
    -->
    <uses-feature android:name="android.hardware.type.watch" />

    <!-- Real-time HR + accelerometer access -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <!--
         BODY_SENSORS_BACKGROUND (Android 13+) so the foreground service keeps
         streaming HR with the screen off / on the wrist.
    -->
    <uses-permission android:name="android.permission.BODY_SENSORS_BACKGROUND" />
    <!-- Foreground service so the watch OS keeps our HR loop alive overnight -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <queries>
        <package android:name="com.google.android.wearable.healthservices" />
    </queries>

    <permission
        android:name="com.example.sleepwisepoc.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.example.sleepwisepoc.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION" />

    <application
        android:allowBackup="true"
        android:appComponentFactory="androidx.core.app.CoreComponentFactory"
        android:debuggable="true"
        android:extractNativeLibs="false"
        android:label="SleepWise"
        android:theme="@android:style/Theme.DeviceDefault" >

        <!--
             Companion-app marker. standalone=false ties the watch APK to the
             phone APK with the same applicationId (com.example.sleepwisepoc).
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="false" />

        <!-- Tiny launcher activity (just "Streaming HR…" / "Idle"). -->
        <activity
            android:name="com.example.sleepwisepoc.wear.MainActivity"
            android:exported="true"
            android:label="SleepWise" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
             Foreground service that registers MeasureClient for HR and forwards
             values to the phone via MessageClient. Started by the phone app
             through Wearable Data Layer when tracking begins.
        -->
        <service
            android:name="com.example.sleepwisepoc.wear.HrStreamService"
            android:exported="false"
            android:foregroundServiceType="health" />

        <!--
             Receives "start streaming" / "stop streaming" messages from the
             phone via Wearable Data Layer.
        -->
        <service
            android:name="com.example.sleepwisepoc.wear.PhoneCommandListener"
            android:exported="true" >
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />

                <data
                    android:host="*"
                    android:pathPrefix="/sleepwise/cmd"
                    android:scheme="wear" />
            </intent-filter>
        </service>

        <uses-library
            android:name="com.google.android.wearable"
            android:required="false" />

        <service
            android:name="androidx.health.services.client.VersionApiService"
            android:exported="true"
            android:permission="com.google.android.wearable.healthservices.permission.PASSIVE_DATA_BINDING" >
            <intent-filter>
                <action android:name="hs.versionclient.BIND" />
            </intent-filter>
        </service>

        <activity
            android:name="com.google.android.gms.common.api.GoogleApiActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="com.example.sleepwisepoc.androidx-startup"
            android:exported="false" >
            <meta-data
                android:name="androidx.profileinstaller.ProfileInstallerInitializer"
                android:value="androidx.startup" />
        </provider>

        <receiver
            android:name="androidx.profileinstaller.ProfileInstallReceiver"
            android:directBootAware="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.DUMP" >
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.INSTALL_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SKIP_FILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SAVE_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.BENCHMARK_OPERATION" />
            </intent-filter>
        </receiver>
    </application>

</manifest>